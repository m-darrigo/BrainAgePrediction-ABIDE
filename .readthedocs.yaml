# .readthedocs.yaml

version: 2

sphinx:
  configuration: docs/conf.py

python:
  version: 3.8
  install:
    - requirements: docs/requirements.txt

os:
  - windows
    build: true
  - linux
    build: true
